// src/utils/ztoolkit.ts
//
// Zotero 8–native build: we do not depend on zotero-plugin-toolkit.
// Keep this module as a stub in case other files import it.

export function createZToolkit() {
  // If you still reference addon.data.ztoolkit anywhere, you can either
  // remove those references or build small helpers yourself.
  return null;
}
